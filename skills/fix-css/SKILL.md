## CSS Fix Workflow
1. First, read the component file and identify the current styles
2. Use browser DevTools-style reasoning: check parent layout, computed values, and inheritance
3. Identify the ROOT CAUSE before proposing any fix
4. Do NOT modify parent component styles unless explicitly asked
5. Make the minimal change needed
6. Use className pattern with CSS custom properties for responsive values